const CONFIG = {
  apps: [{
    "name": "admin",
    "title": "Admin Panel"
  }],
  // node_modules to run through babel
  linkedModules: [
    'template-tools',
    'template-ui',
    'shared'
  ]
}

module.exports = CONFIG